require 'rubygems'
require 'rake'
require 'copycasts'

desc 'Download videos'
task :download do |t, args|
  # options = {}
  # options[:page] = args[:page]  if args[:page]
  copycast_obj = CopyCasts.new
  copycast_obj.download_videos
end
